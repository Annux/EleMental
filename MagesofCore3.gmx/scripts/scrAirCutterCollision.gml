if ((caster = obj_player.id && other.id != obj_player.id) || (caster != obj_player.id && other.id = obj_player.id))
{
    if (image_index = 3) || (image_index = 8)
    {
        critChance = random(100);
        if (critChance <= 10)
        {
            Damage = other.MaxHP*0.25;
        }
        other.HP -= Damage;
    }
}
